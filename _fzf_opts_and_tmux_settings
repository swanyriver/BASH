FZF_TMUX=1 # use fzf-tmux if $TMUX
FZF_CTRL_T_COMMAND='fd -t f . --strip-cwd-prefix' # Use fd for file_select
FZF_TMUX_OPTS='-p 90%,90% --layout=default' # A window that ocupies most of the screen, layout option keeps prompt at bottom.
FZF_CTRL_T_OPTS='--preview="if [[ $(basename {}) =~ ^_ ]]; then batcat --language=sh --color=always {}; else batcat --color=always {}; fi"'

