FZF_TMUX=1 # use fzf-tmux if $TMUX
FZF_CTRL_T_COMMAND='fd -t f . --strip-cwd-prefix' # Use fd for file_select
FZF_TMUX_OPTS='-p 90%,90%' # A window that ocupies most of the screen
FZF_ALT_C_OPTS='--layout=default' # keep prompt at bottom
FZF_CTRL_T_OPTS='--layout=default --preview="if [[ $(basename {}) =~ ^_ ]]; then batcat --language=sh --color=always {}; else batcat --color=always {}; fi"'

